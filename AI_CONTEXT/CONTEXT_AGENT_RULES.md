ALWAYS begin by reading the AI_CONTEXT folder before scanning project files.

Use the AI_CONTEXT files as the primary source of architectural and project understanding.

Only read additional project files when:
- implementation details are required
- debugging requires exact logic
- the requested task is not covered in .context

Minimize unnecessary file reads and optimize for low context usage.

Prefer targeted file inspection over broad repository scanning.